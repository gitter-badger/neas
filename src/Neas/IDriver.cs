using System;

namespace Neas
{
    /// <summary>
    /// Interface for component and device drivers. This includes drivers for
    /// binary devices on the same hardware as well as connected devices via 
    /// USB or TCP.
    /// Dedicated APIs are created by creating derived driver interfaces.
    /// </summary>
    public interface IDriver
    {
    }
}

